🎵 Music Database Project (Backend)
Данный репозиторий содержит реализацию серверной части (Core) музыкальной базы данных на языке C и ресурсы для графического интерфейса.

## Структура проекта

```
project/
├── c_core/                    # Основная логика на языке C
│   ├── include/
│   │   ├── record.h           # Описание структуры Song (100% совместимость с GUI)
│   │   ├── file_handler.h     # Протоколы функций для работы с файлами
│   │   ├── sort.h              
│   │   ├── binary_search.h           
│   │   └── optimal_tree.h            
|   |
│   ├── src/
│   │   ├── file_handler.c     # Реализация загрузки, сохранения и создания бэкапов
│   │   ├── sort.c             # Алгоритм быстрой сортировки
│   │   ├── binary_search.c    # Алгоритм бинарного поиска
│   │   └── optimal_tree.c     # Алгоритм дерева оптимального поиска
|   |
│   └── CMakeLists.txt         # Скрипт для сборки статической библиотеки libcore.a
│
├── data/                      # Бинарные данные
│   ├── music_db.dat           # Основная база (22 записи)
│   └── generate_test_data.c   # Утилита для пересоздания / генерации тестовой базы
│
├── covers/                    # Обложки альбомов (используются в GUI)
│
└── cpp_gui/                   # Исходный код графического интерфейса на C++ (ImGui)
```

## Компиляция и запуск
### 1. Сборка Backend библиотеки
Для того чтобы алгоритмы были доступны в GUI, их нужно собрать в статическую библиотеку:

```Bash
cd c_core
mkdir build && cd build
cmake ..
make
```
После этого в папке build появится файл `libcore.a`.

### 2. Генерация тестовых данных
Если нужно обновить базу данных или добавить новые песни:

```Bash
gcc data/generate_test_data.c -I./c_core/include -o data_gen
./data_gen
```

### 3. Сборка GUI
Для запуска GUI:
cd cpp_gui/architecture
make
make run

Статус разработки (Человек №1)
[x] Проектирование структуры Song.
[x] Реализация работы с бинарным файлом (чтение/запись).
[x] Создание тестовой базы на 22 записи.
[x] Подготовка структуры для Человека №2 (алгоритмы).

Статус разработки (Человек №2)
[x] QuickSort по автору
[x] QuickSort по названию песни
[x] Binary Search по названию
[x] Optimal Binary Search Tree (по названию)
[ ] Тесты и примеры использования

Статус разработки (Человек №3)
[x] Отображение базы данных
[x] Добавление записи
[x] Обработка сортировки
[x] Обработка поиска